node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
# Локальный кэш скрипта генерации базы продуктов (USDA API-ответы) и логи.
# Содержат результаты запросов к USDA — не нужны в репозитории, только локально.
scripts/.usda-cache.json
scripts/.build-log.txt
